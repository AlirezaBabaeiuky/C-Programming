
#include <stdio.h>
#define PI 3.14
int main()
{
    printf("Hello World");
    printf("\nEnter radius of the cicrle here: ");
    int r;
    scanf("%d", &r);
    double area;
    area = PI * pow(r, 2);
    printf("\nArea of the circle is: %lf", area);
    
    return 0;
}
